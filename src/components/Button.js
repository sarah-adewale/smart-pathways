import React from "react";

function Button({children, style}){

    return (
        <>
            
            <button className="" style={style}>{children}</button>
        </>
    )
}

export default Button